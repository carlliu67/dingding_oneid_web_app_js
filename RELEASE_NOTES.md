# Release Notes

## [1.2.1]

### 钉钉日程增加展示腾讯会议号信息
- 创建/更新钉钉日程时，在日程描述中展示腾讯会议号及"加入会议"链接

## [1.2.0]

### 人员选择模式
- 预约/修改会议选择主持人、参会人支持两种模式，由 `REACT_APP_USER_SELECTOR_MODE` 配置
- 全量模式（full）：使用钉钉通讯录组件选人，无范围限制
- 严格模式（strict）：自研组织架构树选人，范围限制为用户所在部门及子部门（支持多部门用户多根展示）
- 严格模式支持部门节点异步加载直属成员、全企业用户搜索（展示姓名、头像、工号）

### 组织架构缓存
- 新增组织架构缓存服务，采用 内存 → 数据库 → 钉钉 三层结构
- 使用独立数据表存储（不复用配置表）：`org_dept_tree`（全量部门树）、`org_dept_users`（部门成员按需加载）；MySQL 使用 MEDIUMTEXT，SQLite 使用独立 `org.db` 库文件
- 启动时若数据库中的部门树未超过24小时则直接加载，否则从钉钉拉取全量组织架构
- 每日凌晨1点（东八区）定时全量刷新组织架构，刷新失败30分钟后自动重试
- 部门成员按需惰性加载：用户展开部门时才拉取，24小时缓存，过期后先返回旧数据再后台刷新
- 钉钉API调用并发限流（5）+ 失败重试；构建失败不落缓存，防止残缺数据被缓存
- 部门被删除后自动清理残留的部门成员缓存（内存+数据库）

### 管理后台
- 新增管理后台页面，仅钉钉企业管理员可访问
- 支持在线配置所有环境参数，AES-256-CBC 加密存储到数据库，保存后实时生效（前端变量除外）
- 参数按分组折叠展示，每组独立校验与保存，仅检测到变更时显示保存按钮
- 支持将工作台应用首页直接配置为管理后台页面

### 时区修复
- 修复钉钉日程时间偏差8小时的问题（ISO 时间按东八区输出）
- 修复机器人云录制消息、待办标题中会议时间偏差8小时的问题
- 修复周期会议重复规则的"每月几号"、结束日期在 UTC 跨日时错位的问题
- Docker 容器时区统一设置为 Asia/Shanghai

### 其他修复与优化
- 修复启动时引用不存在模块导致服务崩溃的问题
- 修复管理页面保存单个分组配置时校验所有分组字段的问题
- MySQL 配置表 value 列自动迁移为 TEXT，支持存储较长配置值
- 严格模式选人树优化：部门直属用户显示在子部门之前、用户节点不再显示展开箭头、跨部门重复用户去重
- 优化组织架构构建性能：子部门信息随 listsub 一次获取（调用量减半）、同层并行展开

### 数据自动过期/定时清理
- 新增数据定时清理功能，每日凌晨2点自动清理过期数据，避免数据表无限膨胀

### 支持免费账号可以预约会议
- 免费账号预约会议不支持指定主持人和设置参会类型


## [1.1.0]

### 前后端同端口部署
- 后端 Koa 服务同时托管前端静态文件，前后端复用同一端口（默认 7000），简化部署与域名配置
- 前端改用相对路径访问后端 API，消除跨端口跨域问题
- Docker、docker-compose、nginx 示例配置统一为单端口

### 开发模式
- 新增 `npm run start:dev` 开发命令，并行启动前后端
- 开发模式下前端 dev server 占用 7000，后端运行在内部端口 7001，通过 proxy 代理 `/api`，用户侧统一访问 7000
- 生产模式 `npm start` 构建前端并由后端托管静态文件

### 配置清理
- 移除前端无用的 `REACT_APP_SERVER_URL`、`REACT_APP_SERVER_PROTOCOL`、`REACT_APP_API_PORT` 配置
- 移除后端无用的日志配置（`ENABLE_FRONTEND_LOG`、`FRONTEND_LOG_MAX_SIZE`、`FRONTEND_LOG_FLUSH_INTERVAL`）

## [1.0.6]

### 鸿蒙端适配
- 支持鸿蒙（HarmonyOS）端正常使用，可识别纯血鸿蒙与已开「兼容模式」两种状态。

### 加入会议
- 纯血鸿蒙（未开兼容）：从日程/待办打开应用不再自动跳转，显示应用首页；会议列表点「加入会议」直接打开参会链接。
- 鸿蒙兼容模式：与 Android、iOS 一致，走通用入会流程。
- PC 端：保持原有唤起客户端方式。

### 兼容模式引导
- 纯血鸿蒙每次打开应用都会弹窗，引导用户点击右上角「⋯」切换到兼容模式。
- 已开启兼容模式或非鸿蒙端不再弹窗。

## [1.0.5]

### 主要功能
- 腾讯会议客户端创建/修改/取消会议支持同步到钉钉日程/待办
- 支持周期会议同步到钉钉日程
- 工作台应用支持预约、加入会议、直接唤起腾会客户端等模式
- 预约会议支持参会类型、水印等会议参数设置
- PC端支持从日程、待办及工作台应用入口免登跳转入会
- 移动端支持从日程、待办及工作台应用入口经系统浏览器跳转免登入会（钉钉限制无法直接跳转三方应用）
- 云录制文件生成IM消息推送与免登打开云录制页面
- 腾讯会议通讯录对接模式支持OneID或者iDaaS
- 配置redis后支持多节点部署
- 数据库支持mysql和sqlite
- 工作台应用页面高级账号支持创建会议，免费账号隐藏创建会议入口


